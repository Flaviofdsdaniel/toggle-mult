apiVersion: v1
kind: Service

metadata:
  name: targeting-service
  namespace: toggle-mult

spec: 
  type: ClusterIP
  selector:
    app: targeting-service
  
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003